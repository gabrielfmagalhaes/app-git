Just applying some vanilla JS to fetch an API.

Returns a list of repositories (public) from an account in GitHub


## Running
```
node server.js
```

## API's
```
GET
localhost:3000/app-git-repositories/users/:username
```